<h1> Oldest director </h1>

<p>The oldest director in our dataset right now is 
  <a href="/directors/<%= @the_oldest_director.id %>">
    <%= @the_oldest_director.name %>
  </a>, born on <%= @the_oldest_director.dob.strftime("%B %d, %Y") %>.
</p>

<hr>

<p>
  (<a href="http://strftime.net/" target="_blank">http://strftime.net/</a> is handy for putting together the formatting strings for the <code>.strftime</code> method.)
</p>
